# all environment vars used for local docker compose setup
ELASTIC_CLUSTER_NAME=covid-docker-cluster
ELASTIC_STACK_VERSION=8.18.1
ELASTIC_PASSWORD=changeme
ELASTIC_LICENSE=basic
KIBANA_PASSWORD=changeme
KIBANA_PORT=5601
ES_PORT=9200
# Increase or decrease based on the available host memory (in bytes)
ES_MEM_LIMIT=1073741824  # ~1gb
